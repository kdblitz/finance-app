version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6
    steps:
      - checkout
      - run: 
          name: Installing deps
          command: npm install
      - run:
          name: Build and deploy app
          command: ./scripts/deploy.sh